<div class="min-h-screen bg-gray-900 py-8">
  <div class="container mx-auto px-4 sm:px-6 lg:px-8">
    <div class="bg-gray-800 shadow-xl rounded-lg">
      <div class="p-6 sm:p-8">
        <h1 class="text-3xl font-bold text-white mb-8">Labor Party Supporter Survey</h1>
        
        <%= render 'survey_responses/progress' %>

        <%= form_with model: @survey_response, url: update_survey_step_path(@step), method: :patch, class: "space-y-6" do |f| %>
          <% if @survey_response.errors.any? %>
            <div class="bg-red-900 p-4 rounded-md">
              <div class="text-sm text-red-200">
                <h2><%= pluralize(@survey_response.errors.count, "error") %> prohibited this survey from being saved:</h2>
                <ul class="list-disc pl-5">
                  <% @survey_response.errors.full_messages.each do |msg| %>
                    <li><%= msg %></li>
                  <% end %>
                </ul>
              </div>
            </div>
          <% end %>

          <div class="space-y-6">
            <div>
              <%= f.label :interest, "Why are you interested in supporting a new Labor Party?", class: "block text-sm font-medium text-gray-300" %>
              <%= f.text_area :interest, rows: 4, class: "mt-1 block w-full rounded-md border-gray-600 bg-gray-700 text-white shadow-sm focus:border-indigo-500 focus:ring-indigo-500 sm:text-sm" %>
            </div>

            <div>
              <%= f.label :values, "What values or principles are most important to you?", class: "block text-sm font-medium text-gray-300" %>
              <%= f.text_area :values, rows: 4, class: "mt-1 block w-full rounded-md border-gray-600 bg-gray-700 text-white shadow-sm focus:border-indigo-500 focus:ring-indigo-500 sm:text-sm" %>
            </div>

            <div>
              <%= f.label :goals, "What do you envision as the primary goals of a new Labor Party?", class: "block text-sm font-medium text-gray-300" %>
              <%= f.text_area :goals, rows: 4, class: "mt-1 block w-full rounded-md border-gray-600 bg-gray-700 text-white shadow-sm focus:border-indigo-500 focus:ring-indigo-500 sm:text-sm" %>
            </div>
          </div>

          <div class="pt-6 flex justify-between">
            <%= link_to "← Previous", survey_step_path('contact'), class: "px-4 py-2 border border-gray-600 rounded-md shadow-sm text-sm font-medium text-gray-300 bg-gray-700 hover:bg-gray-600 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500 focus:ring-offset-gray-900" %>
            <%= f.submit "Next Step →", class: "px-4 py-2 border border-transparent rounded-md shadow-sm text-sm font-medium text-white bg-indigo-600 hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500 focus:ring-offset-gray-900" %>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</div> 